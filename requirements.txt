# Core 
numpy
pypdf
pytest

# Optional — uncomment to use real backends instead of the test/demo stand-ins
# sentence-transformers   # needed for EMBEDDING_BACKEND=sentence-transformers
# anthropic               # needed for LLM_BACKEND=anthropic